# Dot in front of the job will hide it from the CI
.setup_gpu:
  gpu_runner_image: &gpu-runner-image
    image: mambaorg/micromamba:jammy-cuda-12.3.1
    tags:
      - saas-linux-medium-amd64-gpu-standard
  before_script_gpu: &gpu-before-script
    - python --version  # For debugging
    - nvidia-smi  # Nvidia GPU information
    - pip install .[dev]
    # make sure PyTorch us using the GPUs
    - python -c "import torch; exit('PyTorch did not find a GPU') if not torch.cuda.is_available() else print('PyTorch is using GPU')"

.setup_cpu:
  image: &cpu-runner-image mambaorg/micromamba:jammy
  before_script: &cpu-before-script
    - python --version  # For debugging
    - pip install .[dev]

.setup_test_artifacts: &setup-artifacts
  paths:
    - tests/test_artifacts/  # Or the directory where plots are saved
  expire_in: 1 week  # Optional: set an expiration time for artifacts
  when: on_failure  # Save artifacts only on failure

test_py3_11_gpu:
  stage: test
  <<: *gpu-runner-image
  before_script:
    - micromamba install -y -n base -c conda-forge 'python=3.11'
    - *gpu-before-script
  script: python -m pytest --verbose
  artifacts: *setup-artifacts

test_py3_11_cpu:
  stage: test
  image: *cpu-runner-image
  before_script:
    - micromamba install -y -n base -c conda-forge 'python=3.11'
    - *cpu-before-script
  script: python -m pytest --verbose
  artifacts: *setup-artifacts

test_py3_10_gpu:
  when: manual
  stage: test
  <<: *gpu-runner-image
  before_script:
    - micromamba install -y -n base -c conda-forge 'python=3.10'
    - *gpu-before-script
  script: python -m pytest --verbose
  artifacts: *setup-artifacts

test_py3_10_cpu:
  when: manual
  stage: test
  image: *cpu-runner-image
  before_script:
    - micromamba install -y -n base -c conda-forge 'python=3.10'
    - *cpu-before-script
  script: python -m pytest --verbose
  artifacts: *setup-artifacts

test_py3_9_gpu:
  when: manual
  stage: test
  <<: *gpu-runner-image
  before_script:
    - micromamba install -y -n base -c conda-forge 'python=3.9'
    - *gpu-before-script
  script: python -m pytest --verbose
  artifacts: *setup-artifacts

test_py3_9_cpu:
  when: manual
  stage: test
  image: *cpu-runner-image
  before_script:
    - micromamba install -y -n base -c conda-forge 'python=3.9'
    - *cpu-before-script
  script: python -m pytest --verbose
  artifacts: *setup-artifacts

test_code_formatting:
  stage: test
  image: *cpu-runner-image
  before_script:
    - python -m pip install pre-commit
    - pre-commit install
  script:
    - pre-commit run --all-files

docs:
  stage: test
  image: *cpu-runner-image
  before_script:
    - micromamba install -y -n base -c conda-forge 'python=3.11' make pandoc
    - *cpu-before-script
  script:
    - cd docs
    - make html
  artifacts:
    paths:
      - docs/_build